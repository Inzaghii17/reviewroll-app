# ReviewRoll React Frontend (Migration Skeleton)

This folder contains the React app used for incremental migration from the current vanilla SPA.

## Run

```bash
npm install
npm run dev
```

Default dev URL:
- http://localhost:5173

Configure Flask API base in .env:

```bash
copy .env.example .env
```
